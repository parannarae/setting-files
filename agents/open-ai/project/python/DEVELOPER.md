# ~/DEVELOPER.md

## General
- If the project has no project manager or linter, prefer `uv` and `ruff`, respectively, and add them as dependencies.

## Tests
- Keep tests under `tests` in a structure that mirrors source ownership. Keep shared test utilities in a clearly named common utility location.
